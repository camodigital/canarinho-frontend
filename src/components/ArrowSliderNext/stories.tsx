import { Story, Meta } from '@storybook/react/types-6-0'
import ArrowSliderNext from '.'

export default {
  title: 'ArrowSliderNext',
  component: ArrowSliderNext
} as Meta

export const Default: Story = () => <ArrowSliderNext />
