import media from './_respondTo'
import fonts from '../props/_fonts'

export const fontS1 = () => {
  return `
    font-size: ${fonts.sizes.s1.phone};

    ${media.tablet} {
      font-size: ${fonts.sizes.s1.tablet};
    }

    ${media.desktop} {
      font-size: ${fonts.sizes.s1.desktop};
    }

    ${media.wide} {
      font-size: ${fonts.sizes.s1.wide};
    }

    ${media.superwide} {
      font-size: ${fonts.sizes.s1.superwide};
    }

    ${media.ultrawide} {
      font-size: ${fonts.sizes.s1.ultrawide};
    }
  `
}

export const fontS2 = () => {
  return `
    font-size: ${fonts.sizes.s2.phone};

    ${media.tablet} {
      font-size: ${fonts.sizes.s2.tablet};
    }

    ${media.desktop} {
      font-size: ${fonts.sizes.s2.desktop};
    }

    ${media.wide} {
      font-size: ${fonts.sizes.s2.wide};
    }

    ${media.superwide} {
      font-size: ${fonts.sizes.s2.superwide};
    }

    ${media.ultrawide} {
      font-size: ${fonts.sizes.s2.ultrawide};
    }
  `
}

export const fontS3 = () => {
  return `
    font-size: ${fonts.sizes.s3.phone};

    ${media.tablet} {
      font-size: ${fonts.sizes.s3.tablet};
    }

    ${media.desktop} {
      font-size: ${fonts.sizes.s3.desktop};
    }

    ${media.wide} {
      font-size: ${fonts.sizes.s3.wide};
    }

    ${media.superwide} {
      font-size: ${fonts.sizes.s3.superwide};
    }

    ${media.ultrawide} {
      font-size: ${fonts.sizes.s3.ultrawide};
    }
  `
}

export const fontS4 = () => {
  return `
    font-size: ${fonts.sizes.s4.phone};

    ${media.tablet} {
      font-size: ${fonts.sizes.s4.tablet};
    }

    ${media.desktop} {
      font-size: ${fonts.sizes.s4.desktop};
    }

    ${media.wide} {
      font-size: ${fonts.sizes.s4.wide};
    }

    ${media.superwide} {
      font-size: ${fonts.sizes.s4.superwide};
    }

    ${media.ultrawide} {
      font-size: ${fonts.sizes.s4.ultrawide};
    }
  `
}

export const fontS5 = () => {
  return `
    font-size: ${fonts.sizes.s5.phone};

    ${media.tablet} {
      font-size: ${fonts.sizes.s5.tablet};
    }

    ${media.desktop} {
      font-size: ${fonts.sizes.s5.desktop};
    }

    ${media.wide} {
      font-size: ${fonts.sizes.s5.wide};
    }

    ${media.superwide} {
      font-size: ${fonts.sizes.s5.superwide};
    }

    ${media.ultrawide} {
      font-size: ${fonts.sizes.s5.ultrawide};
    }
  `
}

export const fontS6 = () => {
  return `
    font-size: ${fonts.sizes.s6.phone};

    ${media.tablet} {
      font-size: ${fonts.sizes.s6.tablet};
    }

    ${media.desktop} {
      font-size: ${fonts.sizes.s6.desktop};
    }

    ${media.wide} {
      font-size: ${fonts.sizes.s6.wide};
    }

    ${media.superwide} {
      font-size: ${fonts.sizes.s6.superwide};
    }

    ${media.ultrawide} {
      font-size: ${fonts.sizes.s6.ultrawide};
    }
  `
}

export const fontS7 = () => {
  return `
    font-size: ${fonts.sizes.s7.phone};

    ${media.tablet} {
      font-size: ${fonts.sizes.s7.tablet};
    }

    ${media.desktop} {
      font-size: ${fonts.sizes.s7.desktop};
    }

    ${media.wide} {
      font-size: ${fonts.sizes.s7.wide};
    }

    ${media.superwide} {
      font-size: ${fonts.sizes.s7.superwide};
    }

    ${media.ultrawide} {
      font-size: ${fonts.sizes.s7.ultrawide};
    }
  `
}

export const fontS8 = () => {
  return `
    font-size: ${fonts.sizes.s8.phone};

    ${media.tablet} {
      font-size: ${fonts.sizes.s8.tablet};
    }

    ${media.desktop} {
      font-size: ${fonts.sizes.s8.desktop};
    }

    ${media.wide} {
      font-size: ${fonts.sizes.s8.wide};
    }

    ${media.superwide} {
      font-size: ${fonts.sizes.s8.superwide};
    }

    ${media.ultrawide} {
      font-size: ${fonts.sizes.s8.ultrawide};
    }
  `
}
